package com.vettriage.security;

// Replaced by FirebaseAuthFilter — kept as dead code to avoid breaking build.
// Do not annotate with @Component; FirebaseAuthFilter handles both Firebase and JWT tokens.
